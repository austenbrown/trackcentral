<h1>Editing track</h1>

<% form_for(@track) do |f| %>
	<%= render :partial => "form", :locals => {:f  => f} %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @track %> |
<%= link_to 'Back', tracks_path %>